# ========== 1. 启动 8 个 vLLM 实例 ==========
[runtime.raw]
config = [
    { app = "client_toB", background = false },
]

[variables]
model_path = "${model-path}"
blitzdir = "${work-dir}"
tokenizer_path = "${model-path}/tokenizer.json"
vllm_executable = "${venv-path}/bin/vllm serve"
client_executable = "${work-dir}/target/release/client"
base_vllm_port = 59180
dataset_dir = "${dataset-dir}"
log_dir = "${log-dir}"
output_dir = "${output-dir}"
toB_trace_name = "qwen_traceB_blksz_16.jsonl"
tokenizer_config_path = "${model-path}/tokenizer_config.json"
num_producer = 12
channel_capacity = 16


# ========== 通用客户端模板 ==========
[app.client_template]
executable = ["${client_executable}"]
extra_args = ["--replay-mode"]

[app.client_template.macro_rules]
"--tokenizer" = "tokenizer"
"--endpoint" = "endpoint"
# "--protocol" = "protocol"
"--scale-factor" = "scale_factor"
"--api" = "api"
# "--dataset-type" = "dataset_type"
"--dataset" = "dataset_type"
"--dataset-path" = "dataset_path"
"--time-in-secs" = "time_in_secs"
# "--truncate" = "truncate"
"--output-path" = "output_path"
"--num-producer" = "num_producer"
"--channel-capacity" = "channel_capacity"
"--tokenizer-config" = "tokenizer_config"


# ========== 具体客户端实例 ==========
[app.client_toB]
inherit = "client_template"
config.tokenizer = "${tokenizer_path}"
config.tokenizer_config = "${tokenizer_config_path}"
config.num_producer = "${num_producer}"
config.channel_capacity = "${channel_capacity}"
config.endpoint = "http://localhost:58009/generate"
config.protocol = "st"
config.api = "tgi"
config.scale_factor = 1.0
config.dataset_type = "bailian"
config.dataset_path = "${dataset_dir}/${toB_trace_name}"
config.time_in_secs = 600
config.truncate = 16000
config.output_path = "${output_dir}/client_toB.jsonl"
